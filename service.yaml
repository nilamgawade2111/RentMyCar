apiVersion: v1
kind: Service
metadata:
  name: rentmycar
  namespace: default
  labels:
    app: rentmycar
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: rentmycar

