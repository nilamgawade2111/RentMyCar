apiVersion: v1
kind: Service
metadata:
  name: rentmycar
  namespace: default
  labels:
    app: rentmycar
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: rentmycar